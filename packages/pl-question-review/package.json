{
  "name": "pl-question-review",
  "version": "0.1.0",
  "description": "Reusable review workflow logic for PrairieLearn question review.",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "test": "node --test ./test/*.test.js",
    "check": "node --check ./src/index.js && node --check ./src/manifest-adapter.js && npm run test"
  },
  "engines": {
    "node": ">=20"
  }
}
